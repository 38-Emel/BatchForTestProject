string first_name = 'Emel';

string last_name = 'Sarli';
string fullName = first_name + ' ' + last_name;
system.debug('fullname is => ' + fullName);

first_name = 'Eda'; // Reassign
last_name = 'Serim';
system.debug('fullname is => ' + fullName);



string declareVar; // It is null
string blankVar = ' '; // This is blank